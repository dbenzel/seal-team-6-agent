# Project Context

Read `AGENTS.md` or `agents.md` in the project root — either is the entry point for all agentic guidance, engineering best practices, and language-specific conventions.

Always read `docs/agentic/guardrails.md` before taking any actions. Follow other references as they become relevant to your current task — do not pre-read all referenced files.

Pay special attention to:
- The stack detection table — load language guides matching this project's stack
- `.project-context.md` (if it exists) — project-specific context takes precedence
- `docs/agentic/untrusted-input.md` when handling web/MCP/untrusted paste
